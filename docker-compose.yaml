services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: prompt2page-backend
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=AIzaSyAuinUmmxHB_6bHhFSBKXGYat-lM3z7x3k
      - ENVIRONMENT=production
      - PYTHONUNBUFFERED=1
    networks:
      - app-network
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: prompt2page-frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge
